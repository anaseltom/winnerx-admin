import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'admin.winnerx.com',
  appName: 'admin.winnerx.com',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
